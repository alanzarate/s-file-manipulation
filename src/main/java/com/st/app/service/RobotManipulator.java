package com.st.app.service;
import java.awt.*;

public class RobotManipulator {
    private Robot bot;
    public RobotManipulator(Robot... bot){
        if(bot.length > 0 ){
            if(bot)
        }
        if(bot != null){
            this.bot = bot[0];
        }
    }
    public void click(){

    }
}

